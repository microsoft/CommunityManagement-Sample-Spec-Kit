import React from "react";
import type { SkipLinkProps } from "./SkipLink.js";

// Skip links are a web-only concept; this is a no-op on native platforms.
export function SkipLink(_props: SkipLinkProps) {
  return null;
}
